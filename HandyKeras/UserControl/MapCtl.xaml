<UserControl x:Class="HandyKeras.UserControl.MapCtl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource MainContentForegroundDrawingBrush}"
             DataContext="{Binding Map,Source={StaticResource Locator}}"
             d:DesignHeight="500"
             d:DesignWidth="1000">
    <Grid>
        <ContentControl Content="{Binding BezierCurveContainer}"/>
        <ContentControl Content="{Binding MapItemContainer}"/>
    </Grid>
</UserControl>
